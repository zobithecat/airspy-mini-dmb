diff --git a/eti-cmdline/CMakeLists.txt b/eti-cmdline/CMakeLists.txt
index d82d499..0ab4236 100755
--- a/eti-cmdline/CMakeLists.txt
+++ b/eti-cmdline/CMakeLists.txt
@@ -438,7 +438,7 @@ endif()
            if (NOT LIBAIRSPY_FOUND)
                message(FATAL_ERROR "please install airspy library")
            endif ()
-	   ### include_directories (${AIRSPYLIB_INCLUDE_DIR})
+	   include_directories (${LIBAIRSPY_INCLUDE_DIR})
 
 	   include_directories (
 	     ./devices/airspy-handler
@@ -577,11 +577,11 @@ endif()
 	if (RPI_DEFINED)
 	   # detect Raspbian system architecture and set proper options 
 	   # test options with "make VERBOSE=1"
-	   if (CMAKE_HOST_SYSTEM_PROCESSOR STREQUAL aarch64)
+	   if (CMAKE_HOST_SYSTEM_PROCESSOR MATCHES "aarch64|arm64")
 	      target_compile_options(${objectName}  PRIVATE -mcpu=native )
 	   else()
 	      target_compile_options(${objectName}  PRIVATE -march=armv7-a -mfloat-abi=hard -mfpu=neon-vfpv4 )
-	   endif()	
+	   endif()
 	endif()
 	target_link_libraries (${objectName}
 #	                       ${FFTW3F_LIBRARIES}
diff --git a/eti-cmdline/devices/airspy-handler/airspy-handler.cpp b/eti-cmdline/devices/airspy-handler/airspy-handler.cpp
index 722fbc3..0c093a3 100755
--- a/eti-cmdline/devices/airspy-handler/airspy-handler.cpp
+++ b/eti-cmdline/devices/airspy-handler/airspy-handler.cpp
@@ -45,6 +45,14 @@ uint32_t samplerate_count;
 #ifdef	__MINGW32__
 	const char *libraryString = "airspy.dll";
 	Handle		= LoadLibrary ((wchar_t *)L"airspy.dll");
+#elif defined(__APPLE__)
+	const char *libraryString = "libairspy.dylib";
+	Handle		= dlopen ("libusb-1.0.dylib", RTLD_NOW | RTLD_GLOBAL);
+	if (Handle == NULL) {
+	   fprintf (stderr, "libusb cannot be loaded\n");
+	   throw (21);
+	}
+	Handle		= dlopen ("libairspy.dylib", RTLD_LAZY);
 #else
 	const char *libraryString = "libairspy.so";
 	Handle		= dlopen ("libusb-1.0.so", RTLD_NOW | RTLD_GLOBAL);
@@ -52,7 +60,7 @@ uint32_t samplerate_count;
 	   fprintf (stderr, "libusb cannot be loaded\n");
 	   throw (21);
 	}
-	   
+
 	Handle		= dlopen ("libairspy.so", RTLD_LAZY);
 #endif
 
@@ -196,7 +204,26 @@ int32_t	bufSize	= EXTIO_NS * EXTIO_BASE_TYPE_SIZE * 2;
 	   return false;
 	}
 
-	result = my_airspy_set_sensitivity_gain (device, gain * 21 / 100);
+	if (gain <= 0) {
+	   // gain == 0 -> hardware AGC: LNA + mixer auto, VGA at midpoint
+	   if (my_airspy_set_lna_agc != NULL)
+	      my_airspy_set_lna_agc (device, 1);
+	   if (my_airspy_set_mixer_agc != NULL)
+	      my_airspy_set_mixer_agc (device, 1);
+	   if (my_airspy_set_vga_gain != NULL)
+	      my_airspy_set_vga_gain (device, 10);
+	   fprintf (stderr, "[airspy] AGC mode (LNA+mixer auto, VGA=10)\n");
+	} else {
+	   // gain is the simplified sensitivity index (0..21)
+	   int sens = gain;
+	   if (sens > 21) sens = 21;
+	   if (my_airspy_set_lna_agc != NULL)
+	      my_airspy_set_lna_agc (device, 0);
+	   if (my_airspy_set_mixer_agc != NULL)
+	      my_airspy_set_mixer_agc (device, 0);
+	   result = my_airspy_set_sensitivity_gain (device, sens);
+	   fprintf (stderr, "[airspy] sensitivity gain = %d (of 21)\n", sens);
+	}
 	result = my_airspy_set_freq (device, frequency);
 	(void) my_airspy_set_rf_bias (device, rf_bias ? 1 : 0);
 	result = my_airspy_start_rx (device,
diff --git a/eti-cmdline/eti-class.cpp b/eti-cmdline/eti-class.cpp
index 79b2916..aabf120 100755
--- a/eti-cmdline/eti-class.cpp
+++ b/eti-cmdline/eti-class.cpp
@@ -48,7 +48,7 @@
 	                                             theMode,
 	                                             the_callBacks,
 	                                             &my_etiGenerator,
-	                                             2, 5, 
+	                                             1, 2,
 	                                             userData) {
 }
 
diff --git a/eti-cmdline/includes/eti-handling/mm_malloc.h b/eti-cmdline/includes/eti-handling/mm_malloc.h
index 199a480..3cfbca9 100755
--- a/eti-cmdline/includes/eti-handling/mm_malloc.h
+++ b/eti-cmdline/includes/eti-handling/mm_malloc.h
@@ -39,6 +39,14 @@ OF THIS SOFTWARE, EVEN IF ADVISED OF THE POSSIBILITY OF SUCH DAMAGE.
 
 #if defined(_WIN32) || defined(_WIN64)
 #define MALLOC(a) _mm_malloc(a, 16)
+#elif defined(__APPLE__)
+#include <stdlib.h>
+static inline void *_tdmb_aligned_malloc(size_t sz) {
+    void *p = NULL;
+    if (posix_memalign(&p, 16, sz) != 0) return NULL;
+    return p;
+}
+#define MALLOC(a) _tdmb_aligned_malloc(a)
 #else
 #include <malloc.h>
 #define MALLOC(a) memalign(16, a)
diff --git a/eti-cmdline/src/eti-handling/eep-protection.cpp b/eti-cmdline/src/eti-handling/eep-protection.cpp
index 58bf042..465af29 100755
--- a/eti-cmdline/src/eti-handling/eep-protection.cpp
+++ b/eti-cmdline/src/eti-handling/eep-protection.cpp
@@ -34,8 +34,8 @@
 	eep_protection::eep_protection (int16_t bitRate,
 	                                int16_t protLevel):
 	                                     protection (bitRate, protLevel) {
-int16_t i, j;
-int16_t viterbiCounter  = 0;
+int32_t i, j;                            // was int16_t — overflows for >85 kbps EEP
+int32_t viterbiCounter  = 0;
 int16_t L1 = 0,
 	L2 = 0;
 int8_t* PI1{nullptr}, * PI2{nullptr}, * PI_X{nullptr};
@@ -150,8 +150,8 @@ int8_t* PI1{nullptr}, * PI2{nullptr}, * PI_X{nullptr};
 
 bool	eep_protection::deconvolve (int16_t *v,
 	                            int32_t size, uint8_t *outBuffer) {
-int16_t i;
-int16_t	inputCounter	= 0;
+int32_t i;                              // was int16_t — overflows for high-bitrate sub-channels
+int32_t	inputCounter	= 0;
 
         (void)size;                     // currently unused
         memset (viterbiBlock. data (), 0,
diff --git a/eti-cmdline/src/eti-handling/eti-generator.cpp b/eti-cmdline/src/eti-handling/eti-generator.cpp
index 2778f97..1ac8534 100755
--- a/eti-cmdline/src/eti-handling/eti-generator.cpp
+++ b/eti-cmdline/src/eti-handling/eti-generator.cpp
@@ -35,7 +35,8 @@
  *    Foundation, Inc., 59 Temple Place, Suite 330, Boston, MA  02111-1307  USA
  */
 
-#define	__PARALLEL__ 0
+// __PARALLEL__ disabled on macOS — was hanging in std::thread join
+// #define	__PARALLEL__ 0
 //
 #include	<iostream>
 #include	"dab-constants.h"
@@ -442,6 +443,13 @@ int	j, k;
 	prot -> deconvolve (&p -> input [p -> start_cu * CUSize],
 	                                 p -> size * CUSize,
 	                                 outVector.get());
+	{
+	   static int _x = 0;
+	   if ((_x++ & 31) == 0) {
+	      fprintf (stderr, "[subCh] nr=%d exit deconvolve\n", nr);
+	      fflush(stderr);
+	   }
+	}
 //
 	for (j = 0; j < 24 *p -> bitRate; j ++) {
 	   outVector [j] ^= desc [j];
@@ -465,8 +473,9 @@ void	etiGenerator::postProcess (uint8_t *theVector, int32_t offset){
 
 void	etiGenerator::startProcessing	(void) {
 	processing. store (true);
-	fprintf (stderr, "yes, here we go\n");
-	amount. store (0);
+	fprintf (stderr, "yes, here we go (amount=%d)\n", amount.load());
+	// keep amount as-is: if the interleave buffer is already filled
+	// while we were waiting for FIC, no need to discard it.
 }
 
 void	etiGenerator::stop	(void) {
diff --git a/eti-cmdline/src/eti-handling/uep-protection.cpp b/eti-cmdline/src/eti-handling/uep-protection.cpp
index 325feaa..8a0de48 100755
--- a/eti-cmdline/src/eti-handling/uep-protection.cpp
+++ b/eti-cmdline/src/eti-handling/uep-protection.cpp
@@ -144,8 +144,8 @@ int16_t	i;
 	uep_protection::uep_protection (int16_t bitRate,
 	                                int16_t protLevel):
 	                                   protection (bitRate, protLevel) {
-int16_t	index, i, j;
-int16_t viterbiCounter  = 0;
+int32_t	index, i, j;                    // was int16_t — overflows for high bitrates
+int32_t viterbiCounter  = 0;
 int16_t         L1;
 int16_t         L2;
 int16_t         L3;
@@ -231,8 +231,8 @@ int8_t          *PI_X;
 
 bool    uep_protection::deconvolve (int16_t *v,
                                     int32_t size, uint8_t *outBuffer) {
-int16_t i;
-int16_t	inputCounter	= 0;
+int32_t i;                                      // was int16_t — overflows for high-bitrate sub-channels
+int32_t	inputCounter	= 0;
 
         (void)size;                     // currently unused
 
diff --git a/eti-cmdline/src/eti-handling/viterbi-spiral/viterbi-spiral.cpp b/eti-cmdline/src/eti-handling/viterbi-spiral/viterbi-spiral.cpp
index bad753d..bfa91c7 100755
--- a/eti-cmdline/src/eti-handling/viterbi-spiral/viterbi-spiral.cpp
+++ b/eti-cmdline/src/eti-handling/viterbi-spiral/viterbi-spiral.cpp
@@ -143,17 +143,20 @@ int16_t	i, state;
 	size	= (size + 16) & ~0xF;
 	vp. decisions = (decision_t  *)_aligned_malloc (size, 16);
 #else
+	// SPIRAL code reads past the natural end of these buffers; doubling
+	// the allocation matches the Windows path (see BIG NOTE above) and
+	// stops the hang/heap corruption observed on macOS arm64.
 	if (posix_memalign ((void**)&data, 16,
-	                        (wordlength + (K - 1))/ 8 + 1)){
+	                        2 * ((wordlength + (K - 1))/ 8 + 1 + 16))){
 	   printf("Allocation of data array failed\n");
 	}
 	if (posix_memalign ((void**)&symbols, 16,
-	                     RATE * (wordlength + (K - 1)) * sizeof(COMPUTETYPE))){
+	                     2 * (RATE * (wordlength + (K - 1)) * sizeof(COMPUTETYPE) + 16))){
 	   printf("Allocation of symbols array failed\n");
 	}
 	if (posix_memalign ((void**)&(vp. decisions),
 	                    16,
-	                    2 * (wordlength + (K - 1)) * sizeof (decision_t))){
+	                    2 * (2 * (wordlength + (K - 1)) * sizeof (decision_t) + 16))){
 	   printf ("Allocation of vp decisions failed\n");
 	}
 #endif
@@ -218,7 +221,6 @@ uint8_t getbit (uint8_t v, int32_t o) {
 
 void	viterbiSpiral::deconvolve	(int16_t *input, uint8_t *output) {
 uint32_t	i;
-
 	init_viterbi (&vp, 0);
 	for (i = 0; i < (uint16_t)(frameBits + (K - 1)) * RATE; i ++) {
 	   int16_t temp = input [i] + 127;
@@ -226,9 +228,6 @@ uint32_t	i;
 //	   if (temp > 255) temp = 255;
 	   symbols [i] = temp;
 	}
-//	if (!spiral)
-//	   update_viterbi_blk_GENERIC (&vp, symbols, frameBits + (K - 1));
-//	else
 	   update_viterbi_blk_SPIRAL (&vp, symbols, frameBits + (K - 1));
 
 	chainback_viterbi (&vp, data, frameBits, 0);
diff --git a/eti-cmdline/src/ofdm/phasereference.cpp b/eti-cmdline/src/ofdm/phasereference.cpp
index 9899652..d911427 100755
--- a/eti-cmdline/src/ofdm/phasereference.cpp
+++ b/eti-cmdline/src/ofdm/phasereference.cpp
@@ -107,7 +107,7 @@ float	Max		= 1.0;
 	   return  - abs (Max * T_u / sum) - 1;
 	}
 	else
-	   return maxIndex;	
+	   return maxIndex;
 }
 //
 #define	SEARCH_RANGE	(2 * 35)
diff --git a/eti-cmdline/src/support/band-handler.cpp b/eti-cmdline/src/support/band-handler.cpp
index 466eec1..172114a 100755
--- a/eti-cmdline/src/support/band-handler.cpp
+++ b/eti-cmdline/src/support/band-handler.cpp
@@ -69,6 +69,14 @@ struct dabFrequencies bandIII_frequencies [] = {
 {"13D",	235776},
 {"13E",	237488},
 {"13F",	239200},
+// Korean T-DMB raster (TV ch 7-13, sub A/B/C; 1.728 MHz spacing inside 6 MHz)
+{"K7A",  175280}, {"K7B",  177008}, {"K7C",  178736},
+{"K8A",  181280}, {"K8B",  183008}, {"K8C",  184736},
+{"K9A",  187280}, {"K9B",  189008}, {"K9C",  190736},
+{"K10A", 193280}, {"K10B", 195008}, {"K10C", 196736},
+{"K11A", 199280}, {"K11B", 201008}, {"K11C", 202736},
+{"K12A", 205280}, {"K12B", 207008}, {"K12C", 208736},
+{"K13A", 211280}, {"K13B", 213008}, {"K13C", 214736},
 {NULL, 0}
 };
 
